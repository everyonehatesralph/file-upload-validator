# Git Workflow Rules

- All changes must go through Pull Requests (PR).
- No direct commits to the main branch.
- Each feature or fix must be done in a separate branch.
- PR must include:
  - Summary of changes
  - Testing evidence
  - Screenshots (if applicable)
- At least one team member must review before merge.
- Only passing code (no errors/tests failing) can be merged.